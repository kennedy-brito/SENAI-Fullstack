const soma = (num1, num2)=>{
  return num1 + num2;
} 

const subtracao = (num1, num2)=>{
  return num1 - num2;
} 

const multiplicacao = (num1, num2)=>{
  return num1 * num2;
} 

const divisao = (num1, num2)=>{
  return num1 / num2;
} 
